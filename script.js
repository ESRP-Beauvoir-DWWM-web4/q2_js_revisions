// < : inferieur, > : superieur, <= : inferieur ou égal, >= : superieur ou égal, == : De même valeur, === : De même valeur et de même type, % : modulo

let num = 2

if ( num % 2 == 0 ) {
    console.log(`${num} est pair`)
} else if ( num % 2 == 1 ) {
    console.log(`${num} est impair`)
}